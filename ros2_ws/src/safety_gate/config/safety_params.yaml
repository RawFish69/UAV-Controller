safety_gate:
  ros__parameters:
    mode: sim  # sim or crsf
    max_tilt_deg: 35.0
    max_body_rate: 10.0  # rad/s
    thrust_min: 0.05
    thrust_max: 0.9
    slew_rate_thrust: 3.0  # per second
    slew_rate_yaw: 5.0  # rad/s per second
    timeout_ms: 200

